# Playground

Next.js app for manually testing Concept Token configurations and prompt variants.

```bash
npm install
npm run dev
```

Create `.env.local` with:

```env
NEXT_PUBLIC_API_BASE=http://127.0.0.1:5000
```
